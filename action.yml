name: 'setup bundletool'
description: 'setup bundletool of android'
author: 'amyu'

inputs:
  version:
    description: 'Bundletool version. If not specified, downloads the latest GitHub release.'
    required: false

runs:
  using: 'node24'
  main: 'dist/index.js'